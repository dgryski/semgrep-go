rules:
  - id: parseint-downcast
    patterns:
      - pattern: |
                      $X, ... = strconv.ParseInt(..., ..., 64)
                      ...
                      int32($X)

    message: "64-bit integer downcast to int32"
    languages: [go]
    severity: ERROR
