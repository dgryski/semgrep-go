rules:
  - id: use-net-errclosed
    patterns:
            - pattern: strings.Contains(err.Error(), $X)
            - metavariable-regex:
                      metavariable: $X
                      regex: '".*closed network connection.*"'
    message: "Use err == net.ErrClosed instead"
    languages: [go]
    severity: ERROR
